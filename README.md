``` bash
#สร้างไฟล์ venv
python -m venv venv

#activate ไฟล์ venv
source venv/bin/activate

#ติดตั้ง dependency
pip install opencv-python
pip install cvzone
pip install deepface

#รันไฟล์
python main.py

